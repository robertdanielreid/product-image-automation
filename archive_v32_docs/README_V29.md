# V29 cloud-execution package

The source catalogue has 20,076 rows. This edition adds a real cloud execution path because the ChatGPT sandbox cannot resolve external product sites.

Primary file: `CLOUD_RUN_INSTRUCTIONS.md`
Workflow: `.github/workflows/acquire-product-images.yml`
Chunk runner: `cloud/run_chunk.sh`

This package does not claim that images are already downloaded. It provides the environment-independent method that can actually acquire them.
